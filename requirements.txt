# Flask API Framework
flask>=2.3.0
flask-cors>=4.0.0

# HTTP Requests
requests>=2.31.0

# System and Path handling
pathlib>=1.0.1

# Date and time utilities
python-dateutil>=2.8.2

# JSON handling (built-in but listed for clarity)
# json - built-in module

# Regular expressions (built-in)
# re - built-in module

# Threading and subprocess (built-in)
# threading - built-in module
# subprocess - built-in module
# signal - built-in module
# os - built-in module
# time - built-in module
# logging - built-in module

# Optional: For enhanced analysis
# tshark and tcpdump should be installed via system package manager:
# sudo apt install tshark tcpdump wireshark-common

# Optional: For monitor mode testing
# iwconfig, iw, aircrack-ng suite should be installed via system:
# sudo apt install aircrack-ng wireless-tools